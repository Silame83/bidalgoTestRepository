The project with content for Jenkins running python script:
  - adding columns into MySQL on LAMP server.....

Also, available all configuration files (Terraform) for launching servers: "Jenkins" and "code server";
